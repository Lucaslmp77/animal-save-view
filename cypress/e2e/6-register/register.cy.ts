describe('Register', () => {

    beforeEach(() => {
        cy.viewport('macbook-15');
        cy.visit('http://localhost:3000/register');
    });

    context('Quando acessar tela de registro', () => {

        it('Deve exibir a tela de login corretamente', () => {
            
        });
    })
})